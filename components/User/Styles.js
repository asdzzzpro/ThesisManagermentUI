import { StyleSheet } from "react-native";

export default StyleSheet.create({
    button: {
        backgroundColor: "blue",
        color: "white",
        textAlign: "center",
        padding: 10
    }
})